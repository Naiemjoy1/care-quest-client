const TestSendData = ({
  handleConfirmBooking,
  finalPrice,
  finalBookingPrice,
  test,
  filteredBookings,
}) => {
  return (
    <div>
      <h2>Test Data Pass</h2>
      <p>Final Price: ${finalBookingPrice}</p>
    </div>
  );
};

export default TestSendData;
